{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently \"cd backend && php artisan serve --host=127.0.0.1 --port=8000\" \"cd frontend && npm run dev -- --port 5173\""
  }
}
